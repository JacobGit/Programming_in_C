//Finding and correcting the syntactic errors

#include <stdio.h>

int main(void) //void must be lowercase and adjacent to main
{
    int sum; //int must be lowercase
    sum = 25 + 37 - 19; //must end with semicolon
    printf ("The answer is %i\n", sum);
    return 0;
}
